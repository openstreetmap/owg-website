name: owg-log
markdown: kramdown
highlighter: rouge
baseurl:
defaults:
  -
    scope:
      path: ""
      type: pages
    values:
      layout: default
  - scope:
     path: ""
     type: posts
    values:
      layout: post
